// Firebase Client SDK - disabled until Firebase project is configured
// When ready, uncomment and set NEXT_PUBLIC env vars

export const auth = null
export const db = null
export const storage = null
